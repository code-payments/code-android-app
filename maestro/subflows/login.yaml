# Login
appId: com.flipcash.app.android
---
- tapOn: Log in
- extendedWaitUntil:
      visible:
          id: seed_input_screen
- tapOn: 
    id: "seed_input_field"
- inputText: ${SEED_PHRASE}
- tapOn: Log in
# Launch lands on the Wallet tab, not the camera (homeRoute = AppRoute.Sheets.Wallet).
- extendedWaitUntil:
      visible:
          id: "wallet_screen"
