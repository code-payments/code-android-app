# Pin Robolectric to SDK 36 so tests don't need @Config(sdk = ...) on every class.
# Robolectric 4.17 supports SDK 37 (= compileSdk/targetSdk); moving the pin is a
# deliberate change to what every unpinned test runs against, so it lands separately.
sdk=36
